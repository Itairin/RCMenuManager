<Window x:Class="RCMenuManager.Views.Dialogs.BackupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:RCMenuManager.Converters"
        Title="备份与日志" Width="1000" Height="640"
        Background="{StaticResource WindowBackground}"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        FontFamily="Segoe UI Variable, Segoe UI, Microsoft YaHei UI, sans-serif" FontSize="13">
    <Window.Resources>
        <conv:CanRestoreConverter x:Key="CanRestoreConv" />
    </Window.Resources>
    <DockPanel Margin="16">
        <DockPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
            <Button DockPanel.Dock="Right" Content="关闭" MinWidth="88" Margin="10,0,0,0"
                    IsCancel="True" Click="CloseButton_Click" />
            <Button DockPanel.Dock="Right" Content="打开备份目录" MinWidth="124"
                    Command="{Binding OpenBackupFolderCommand}" />
            <TextBlock Text="{Binding StatusText}" Style="{StaticResource MutedText}" VerticalAlignment="Center" />
        </DockPanel>

        <TabControl Background="Transparent" BorderThickness="0">
            <TabItem Header="备份">
                <Border Style="{StaticResource Card}" Margin="0,8,0,0">
                    <DataGrid ItemsSource="{Binding Backups}" SelectedItem="{Binding SelectedBackup}"
                              BorderThickness="0"
                              AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="160" />
                            <DataGridTextColumn Header="作用域" Binding="{Binding ScopeId}" Width="130" />
                            <DataGridTextColumn Header="Verb" Binding="{Binding VerbName}" Width="170" />
                            <DataGridTextColumn Header="操作" Binding="{Binding Operation}" Width="100" />
                            <DataGridTextColumn Header="状态" Binding="{Binding SuccessText}" Width="80" />
                            <DataGridTextColumn Header="注册表" Binding="{Binding RegistryPath}" Width="*" />
                            <DataGridTemplateColumn Header="操作" Width="220">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="还原" MinWidth="58" Height="26" Padding="8,2" Margin="2,0"
                                                    Style="{StaticResource AccentButton}"
                                                    Command="{Binding DataContext.RestoreCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}">
                                                <Button.IsEnabled>
                                                    <MultiBinding Converter="{StaticResource CanRestoreConv}">
                                                        <Binding Path="Success" />
                                                        <Binding Path="RegistryPath" />
                                                    </MultiBinding>
                                                </Button.IsEnabled>
                                            </Button>
                                            <Button Content="删除" MinWidth="58" Height="26" Padding="8,2" Margin="2,0"
                                                    Style="{StaticResource DangerButton}"
                                                    Command="{Binding DataContext.DeleteBackupCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" />
                                            <Button Content="定位" MinWidth="58" Height="26" Padding="8,2" Margin="2,0"
                                                    Command="{Binding DataContext.RevealInExplorerCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <TabItem Header="日志">
                <Border Style="{StaticResource Card}" Margin="0,8,0,0">
                    <DataGrid ItemsSource="{Binding Operations}" SelectedItem="{Binding SelectedOperation}"
                              BorderThickness="0"
                              AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="160" />
                            <DataGridTextColumn Header="操作" Binding="{Binding Op}" Width="110" />
                            <DataGridTextColumn Header="作用域" Binding="{Binding ScopeId}" Width="110" />
                            <DataGridTextColumn Header="Verb" Binding="{Binding Verb}" Width="150" />
                            <DataGridTextColumn Header="Hive" Binding="{Binding Hive}" Width="80" />
                            <DataGridTextColumn Header="SubKey" Binding="{Binding SubKey}" Width="*" />
                            <DataGridTextColumn Header="状态" Binding="{Binding SuccessText}" Width="80" />
                            <DataGridTextColumn Header="错误" Binding="{Binding Error}" Width="200" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
