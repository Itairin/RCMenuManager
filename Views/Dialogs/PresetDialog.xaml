<Window x:Class="RCMenuManager.Views.Dialogs.PresetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RCMenuManager.ViewModels"
        Title="推荐设置"
        Width="820" Height="640"
        Background="{StaticResource WindowBackground}"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        FontFamily="Segoe UI Variable, Segoe UI, Microsoft YaHei UI, sans-serif" FontSize="13">
    <Window.Resources>
        <Style x:Key="StatusBadge" TargetType="Border" BasedOn="{StaticResource Badge}">
            <Setter Property="Margin" Value="6,0,0,0" />
        </Style>
    </Window.Resources>
    <DockPanel Margin="16">
        <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="导入" MinWidth="88" Margin="0,0,10,0" Command="{Binding ImportCommand}" />
            <Button Grid.Column="1" Content="导出" MinWidth="88" Margin="0,0,10,0" Command="{Binding ExportCommand}" />
            <Button Grid.Column="2" Content="刷新" MinWidth="88" Command="{Binding RefreshCommand}" />
            <CheckBox Grid.Column="4" Content="覆盖已存在的 verb"
                      IsChecked="{Binding OverwriteExisting}" VerticalAlignment="Center" />
        </Grid>

        <Grid DockPanel.Dock="Bottom" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusText}"
                       Style="{StaticResource MutedText}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="应用选中" MinWidth="108" Margin="0,0,10,0"
                    Style="{StaticResource AccentButton}"
                    Command="{Binding ApplySelectedCommand}" IsEnabled="{Binding HasSelection}" />
            <Button Grid.Column="2" Content="关闭" MinWidth="88" IsCancel="True" Click="OnCloseClick" />
        </Grid>

        <Border Style="{StaticResource Card}" Padding="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Groups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:PresetGroup}">
                            <Expander Header="{Binding DisplayName}" IsExpanded="True" Margin="2,2,2,8">
                                <StackPanel>
                                    <Grid Margin="4,0,4,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="勾选条目后点击「应用选中」"
                                                   Style="{StaticResource MutedText}" VerticalAlignment="Center" />
                                        <Button Grid.Column="1" Content="全选" MinWidth="70"
                                                Command="{Binding DataContext.SelectAllInGroupCommand,
                                                          RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}" />
                                    </Grid>
                                    <ItemsControl ItemsSource="{Binding Items}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:PresetItemViewModel}">
                                                <Border BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,1"
                                                        Padding="6,8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>
                                                        <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                  VerticalAlignment="Top" Margin="0,2,8,0" />
                                                        <StackPanel Grid.Column="1">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"
                                                                           Foreground="{StaticResource TextBrush}" />
                                                                <Border Style="{StaticResource StatusBadge}" Background="{StaticResource AccentSubtleBrush}">
                                                                    <TextBlock Text="Shift" FontSize="11" Foreground="{StaticResource AccentHoverBrush}"
                                                                               Visibility="{Binding IsExtended, Converter={StaticResource BoolToVis}}" />
                                                                </Border>
                                                                <Border Style="{StaticResource StatusBadge}" Background="{StaticResource WarnSubtleBrush}">
                                                                    <TextBlock Text="用户" FontSize="11" Foreground="{StaticResource WarnBrush}"
                                                                               Visibility="{Binding IsBuiltIn, Converter={StaticResource InvertBoolToVis}}" />
                                                                </Border>
                                                                <Border Style="{StaticResource StatusBadge}" Background="{StaticResource SuccessSubtleBrush}">
                                                                    <TextBlock Text="已应用" FontSize="11" Foreground="{StaticResource SuccessBrush}"
                                                                               Visibility="{Binding IsApplied, Converter={StaticResource BoolToVis}}" />
                                                                </Border>
                                                                <Border Style="{StaticResource StatusBadge}" Background="{StaticResource DangerSubtleBrush}">
                                                                    <TextBlock Text="已存在" FontSize="11" Foreground="{StaticResource DangerBrush}"
                                                                               Visibility="{Binding State, Converter={StaticResource StateToExistsVis}}" />
                                                                </Border>
                                                                <Border Style="{StaticResource StatusBadge}" Background="{StaticResource DangerSubtleBrush}">
                                                                    <TextBlock Text="错误" FontSize="11" Foreground="{StaticResource DangerBrush}"
                                                                               Visibility="{Binding State, Converter={StaticResource StateToErrorVis}}" />
                                                                </Border>
                                                            </StackPanel>
                                                            <TextBlock Text="{Binding Description}" Style="{StaticResource MutedText}" Margin="0,3,0,0" />
                                                            <TextBlock Text="{Binding CommandPreview}" FontSize="11"
                                                                       FontFamily="Cascadia Mono, Consolas, monospace"
                                                                       Foreground="{StaticResource TextBrush}"
                                                                       TextWrapping="Wrap" Margin="0,3,0,0" />
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </DockPanel>
</Window>
